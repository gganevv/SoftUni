namespace Operations
{
    public class MathOperations
    {
        public int Add(int v1, int v2) => v1 + v2;

        public double Add(double v1, double v2, double v3)
        => v1 + v2 + v3;

        public decimal Add(decimal v1, decimal v2, decimal v3)
        => v1 + v2 + v3;
    }
}